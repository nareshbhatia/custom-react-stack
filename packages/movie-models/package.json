{
  "name": "movie-models",
  "version": "0.0.0",
  "main": "./index.ts",
  "license": "MIT",
  "scripts": {
    "lint": "eslint src/**/*.ts*",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  }
}
